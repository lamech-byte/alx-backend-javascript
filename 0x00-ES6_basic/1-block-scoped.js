export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    // Remove these lines: let task = true; and let task2 = false;
  }

  return [task, task2];
}
